# 🦎 게코 하우스 서바이벌 — 개발 로드맵

> 음식을 먹고 똥 게이지를 채운 뒤, 로봇청소기와 집안의 위협을 피해
> 거실 바닥의 **44%** 를 '똥 땅'으로 만드는 쿼터뷰 3D 생존·영역 확장 게임.

---

## 1. 게임의 핵심 재미

**"내가 칠한 땅이 눈앞에서 지워지는 걸 보면서, 그래도 한 번 더 싸러 가는 재미"**

세 가지 긴장이 동시에 돌아간다.

| 축 | 내용 |
|---|---|
| **축적 vs 침식** | 플레이어는 점(點)으로 칠하고, 청소기는 선(線)으로 지운다. 화면의 초록색이 실시간으로 늘고 줄어드는 것 자체가 피드백이다. |
| **욕심 vs 안전** | 배변 애니메이션 1초는 **무적이 아니다.** 넓은 미개척지 한복판은 가장 많이 벌지만 가장 위험하다. |
| **성장의 역설** | 레벨이 오르면 배변 반경이 커지지만 **히트박스도 커진다.** 강해질수록 눈에 띈다. |

핵심 감정은 "귀엽고 웃긴 겉모습 + 실제로는 계속 쫓기는 압박감"의 낙차다.

---

## 2. 한 판의 핵심 게임 루프

```
        ┌──────────────────────────────────────────────┐
        │                                              │
   [음식 3개 찾아 먹기] ──> [똥 게이지 100 충전]        │
        │  배고픔 +75          똥 신호 ! 표시           │
        │  Age 진행도 +3                                │
        ↓                                              │
   [미개척지로 이동] ──> [Space: 1초 배변] ──> [영역 +14.7셀]
        │  청소기 위치 판단      ※ 이동 불가/무적 아님    ↑
        │                                              │
        ↓                                              │
   [로봇청소기가 계속 영역을 지운다] ───────────────────┘
        │
        ├─ 위험하면 ──> [담요 밑 은신] (6초 후 강아지 경고)
        ├─ 위험하면 ──> [가구 위로 대피] (배변 불가)
        └─ 여유 있으면 ──> [화장실 왕복] (34셀 일괄 확보 + 청소기 감속)

   달성률 44% 도달 ──> STAGE_CLEAR
   하트 0 ──────────> GAME_OVER
```

한 판 기준 **배변 약 30회 / 음식 약 90개 / 소요 6.7분**.
핵심 동사(배변)를 **약 13초에 한 번** 실행하므로 루프가 지루해지지 않는다.

---

## 3. 밸런스 검증표 (§0-1) — **필수**

### 3-1. 계산 모델

§0-1의 기본 모델에 **중첩 손실을 명시적으로 분리**한 형태를 사용한다.

```
dC/dt = G₀·(1 − β·p) − S·p          (p = C/V)
      = G₀ − (S + β·G₀)·p
      = G₀ − S_eff·p                 S_eff ≡ S + β·G₀

평형 점유율   p* = G₀ / S_eff
도달 시간     t  = −(V / S_eff) · ln(1 − 0.44 / p*)
```

| 기호 | 의미 |
|---|---|
| `V` | 유효 셀 수 (전체 − BLOCKED) |
| `G₀` | **점유율 0일 때의** 플레이어 영역 증가율 (셀/초) |
| `β` | 중첩 계수. 기존 영역 위에 겹쳐 싸서 버려지는 비율. 완전 무작위 배치면 1.0, 플레이어가 미개척지를 고르므로 **0.8** 적용 |
| `S` | 청소기가 초당 훑는 유효 바닥 면적 (셀/초) |

> **원문 §0-1 모델과의 차이**: 원문은 `dC/dt = G − S·p`로 중첩 손실을 `G` 안에 상수로 접어 넣었다.
> 그러면 후반부(p가 클 때) 중첩 손실이 과소평가되어 도달 시간이 짧게 나온다.
> 위 형태는 수학적으로 동일한 1차 선형 ODE이며, `β·G₀` 항을 분모로 옮긴 것뿐이라 §0-1의 합격 기준을 그대로 적용할 수 있다.

### 3-2. 기본 제원

| 항목 | 값 | 산출 |
|---|---|---|
| 전체 격자 | **768** 셀 | 32 × 24 |
| 거실 실측 | **16 × 12** world units | 768 × 0.5² |
| BLOCKED (실측) | **89** 셀 (11.6%) | `furnitureLayout.ts` 배치에서 파생. 허용 10~15% ✅ |
| 유효 셀 `V` | **679** 셀 | 768 − 89 |
| 목표 셀 (44%) | **299** 셀 | 679 × 0.44 |

### 3-3. 플레이어 증가율 `G₀`

| 항목 | 값 | 산출 |
|---|---|---|
| 배변 반경 (Lvl 1) | 2.3 셀 | = 1.15 world u (지름 2.3m) |
| 배변 1회 총면적 | 16.6 셀 | π × 2.3² |
| BLOCKED 제외 유효면적 `A_eff` | **14.7** 셀 | 16.6 × 0.884 |
| ├ 음식 3개 이동 | 8.5 초 | (6.5 × 1.12 × 1.25 ÷ 3.2) × 3 |
| ├ 먹기 3회 | 1.8 초 | 0.6 × 3 |
| ├ 미개척지 재배치 | 1.0 초 | 2.5 × 1.25 ÷ 3.2 |
| ├ 배변 애니메이션 | 1.0 초 | |
| └ 리스폰 대기(평균) | 1.1 초 | 동시 2개라 실제로 기다린다 |
| **배변 사이클 `T`** | **13.4 초** | 위 합계 (봇 실측 14.2초, §3-8b) |
| **`G₀`** | **1.10 셀/초** | 14.7 ÷ 13.4 |

`1.10` = 순간 최대 증가율. 실제 순증은 `1.10 × (1 − 0.8p)`로 점유율이 오를수록 감소한다.

### 3-4. 청소기 감소율 `S`

| 항목 | 값 |
|---|---|
| 이동 속도 | 1.2 셀/초 (= 0.6 world u/s) |
| 청소 폭 | 1.2 셀 (반경 0.6셀 = 0.3m) |
| 원시 스윕 | 1.44 셀/초 |
| × 회전 duty (2.5 ÷ 2.9) | 0.862 |
| × 경로 중복 계수 η | 0.85 |
| **`S`** | **1.055 셀/초** |
| **`S_eff` = S + 0.8·G₀** | **1.933 셀/초** |

### 3-5. ✅ 합격 판정

| 항목 | 값 | 기준 | 판정 |
|---|---|---|---|
| **평형 점유율 `p*`** | **0.567 (56.7%)** | > 0.44 | ✅ **여유 29%** |
| **44% 도달 시간 (수치 적분, 성장 반영)** | **399초 = 6.7분** | 5~8분 | ✅ |
| 해석해 (Lvl 1 고정 가정) | 525초 = 8.8분 | — | 참고값 |

> 해석해와 수치 적분이 다른 이유: 해석해는 배변 반경이 2.3셀로 고정된다고 가정하지만,
> 실제로는 Age 성장으로 반경이 2.3 → 2.5 → 2.7로 커진다.
> **수치 적분값(6.7분)이 실제에 가까우므로 이쪽을 기준으로 삼는다.**

### 3-6. 숙련도 민감도 (수치 적분, 레벨 성장 반영)

| 시나리오 | 사이클 | 도달 시간 | 배변 | 음식 | 지워진 셀 | 판정 |
|---|---|---|---|---|---|---|
| 숙련 (−25%) | 11.0초 | **5.3분** | 29 | 87 | 79 | ✅ |
| **기준** | **13.4초** | **6.7분** | **30** | **90** | **99** | ✅ |
| 보통 (+25%) | 15.8초 | 8.3분 | 32 | 96 | 126 | ⚠️ 조금 느림 |
| 미숙 (+60%) | 19.1초 | 10.6분 | 34 | 102 | 162 | ⚠️ 느리지만 클리어 가능 |

**클리어 가능 한계: 배변 사이클 20.5초.** 그보다 느리면 `p*` ≤ 0.44로 수학적 불가능.
기준값 13.4초 대비 **53% 여유**가 있어, 플레이어가 상당히 헤매도 클리어가 보장된다.

> §0-1 의 합격 기준은 **기준 시나리오**의 도달 시간이다.
> 숙련~미숙 편차(−25%\~+60%)는 그 자체로 1.7배가 넘어 5\~8분(1.6배) 구간에
> 통째로 넣을 수 없다. 억지로 맞추려 하면 상수를 계속 흔들게 되므로,
> 넓은 구간에는 "클리어 가능하고 15분을 넘지 않는다"만 요구한다.

레벨업 시점(기준 시나리오): `Lvl2 @ 207초 (24%)` → `Lvl3 @ 354초 (36%)`.
인간 적이 등장하는 Lvl 2가 중반(전체의 47% 지점)에 오므로 난이도 상승 곡선이 자연스럽다.

### 3-7. ⚠️ §4 초기값을 조정한 이유와 조정 내역

**§4의 참고값은 내부적으로 모순이 있다.** "배변 주기 약 15초"를 가정했으나,
같은 표의 `MOVE_SPEED 3.2` / `FOOD_MAX_CONCURRENT 3` / 16×12 거실로 실제 사이클을 계산하면 **약 6.5초**가 나온다.
그 경우 `G₀ ≈ 2.9`, `p* > 1.0` 이 되어 **청소기가 원리적으로 따라잡을 수 없고, 44%를 2.2분에 도달**해
목표 플레이 시간(5~8분)을 크게 벗어난다.

§0-1의 지시대로 **목표 44%는 유지하고** 아래 상수를 조정했다.

| 상수 | §4 원안 | **조정안** | 조정 이유 |
|---|---|---|---|
| `POOP_RADIUS_CELLS` | 3.0 | **2.3** | 1회 면적 25 → 14.7셀. `G₀` 직접 인하 |
| `LEVEL` 배변 반경 | 3.0/3.5/4.0 | **2.3/2.5/2.7** | 위와 동일 비율. 성장 가속 완화 |
| `VACUUM_CLEAN_WIDTH` | 2 (셀) | **`VACUUM_CLEAN_RADIUS_CELLS: 0.6`** (폭 1.2) | 폭 2셀 = 1m는 실제 로봇청소기보다 과대. 연속값이라 미세 조정 가능 |
| `POOP_PER_FOOD` | 50 (음식 2개) | **34** (음식 3개) | 사이클에 이동 1회(+3.6초) 추가. **가장 효과가 큰 레버** |
| `FOOD_MAX_CONCURRENT` | 3 | **2** | 최근접 음식까지 거리 증가 |
| `FOOD_RESPAWN_DELAY` | 4 | **6** | 리스폰 대기 압박 |
| `FOOD_MIN_SPAWN_DIST` | *(없음)* | **6.5** (신설) | 플레이어 근처 스폰 차단. 이동 시간을 **결정적으로** 보장 |
| `FOOD_EAT_TIME` | 0.4 | **0.6** | 3회 × 0.6 = 1.8초 |
| `POOP_ANIM_TIME` | 0.8 | **1.0** | 무방비 시간 확대 = 긴장감 + 사이클 |
| `FOOD_PER_AGE` | 3 | **10** | 한 판 음식이 90개라 원안(3)이면 Age 30까지 감. Lvl2를 중반에 배치 |
| `TOILET_BONUS_RATIO` | 0.10 | **0.05** | 아래 3-8 참조 |
| `VACUUM_SPEED_CELLS` | 1.2 | **1.2 (유지)** | — |

### 3-8. 변기 보너스 손익 검증 (§14 "일반 배변 대비 미세하게 유리")

화장실 왕복 추가 시간 20초, 그동안 거실은 계속 청소된다.

```
변기 순이득 = 보너스셀 − (왕복 중 청소 손실) − (포기한 일반 배변) + (청소기 감속 절약)
```

| `TOILET_BONUS_RATIO` | 보너스 셀 | p=0.30 시 이득비 | p=0.44 시 이득비 | 판정 |
|---|---|---|---|---|
| 0.10 (원안) | 68 | 4.10x | 4.82x | ❌ 압도적 — 변기만 반복하는 게임이 됨 |
| 0.07 | 47 | 2.27x | 2.68x | ❌ 여전히 과함 |
| **0.05 (채택)** | **34** | **1.23x** | **1.43x** | ✅ 초반 호각, 후반 마무리용 |
| 0.04 | 27 | 0.45x | 0.54x | ❌ 갈 이유가 없음 |

**0.05 채택.** 초반에는 일반 배변보다 약간 유리(1.23배)한 정도라 선택의 여지가 있고,
후반(p가 높아 중첩 손실이 큰 시점)에 1.43배로 유리해져 **"막판 승부수"** 역할을 한다.
BFS 인접 확장이므로 보너스 34셀은 중첩 손실이 0인 것이 유리함의 근거다.

### 3-8g. ✅ S9 테스트 정비 — 밸런스 영향 없음

`GameConfig` 상수를 건드리지 않았다. `npm run balance` 결과는 그대로다
(`p* = 0.567`, 도달 399초).

S8 시점에 "E2E 2\~4개가 전체 실행에서만 실패한다"고 적어 두었다. 그 원인을 끝까지
파고든 결과 **넷 중 둘은 테스트가 아니라 제품 코드의 결함**이었다.

| 원인 | 성격 | 조치 |
|---|---|---|
| 시드가 `Date.now()` 뿐이라 밖에서 고정할 수 없음 | **제품 결함** — §0-5 는 "같은 시드면 같은 판"을 요구하는데 그 시드를 지정할 방법이 없었다 | `?seed=` 지원. 재시작 시드도 거기서 파생 |
| `GameLoop.droppedTime` 이 판을 넘겨 누적됨 | **제품 결함** — 로딩·타이틀의 히치가 플레이 성능으로 보고되고, §19 패널 값이 재시작 후 무의미해진다 | `resetStats()` 를 판 시작·재시작에서 호출 |
| 게임 시간으로 정의된 사건을 벽시계로 기다림 | 테스트 결함 | `expectWithinGameTime` — 예산은 게임 시간 |
| `E` 가 무엇을 실행할지 가정함 (음식 우선권을 무시) | 테스트 결함 | `debug.interaction()` 확인 후 입력 |

추가로, 소크 테스트를 고치는 과정에서 **그 테스트가 실은 게임을 돌리지 않고
있었다**는 것이 드러났다. 무작위 보행은 시작점 주변만 맴돌아 음식(6.5 units 밖
스폰)에 닿지 못한다 — `Math.random()` 의 운으로 가끔 통과했을 뿐이다.
음식을 향해 걷게 고치자 "플레이 중 지오메트리 증가"가 새로 보였는데,
이건 누수가 아니라 **`renderer.info.memory` 가 지연 업로드까지 센 것**이었다
(Lvl 2 에서 인간이 등장하면 57 → 59 → 62). 씬 그래프의 리소스 수를 직접 세는
`debug.sceneStats()` 로 바꿔 착시를 제거했다. R5 는 이제 이 값으로 판정한다.

§19 의 "프로덕션 번들에 디버그 UI 미포함"은 S8 에서 문자열 grep 으로만 확인했었다.
이름이 사라져도 코드는 남을 수 있으므로, **빌드 산출물을 실제로 띄워** `` ` `` 를
눌러도 패널이 열리지 않고 `window.__GAME__` 이 없음을 확인하는 E2E 를 추가했다.

### 3-8f. ✅ S8 연출 레이어 — 밸런스 영향 없음

§24 는 확장 기능이 밸런스에 영향을 주면 재검증하라고 요구한다.
S8 은 **`GameConfig` 상수를 하나도 건드리지 않았고**, 추가된 코드는 전부
`EventBus` 를 구독하는 쪽(사운드·파티클·표정)이거나 상태를 읽기만 하는
오버레이(타이틀·튜토리얼·디버그 패널)다. `npm run balance` 결과는 그대로다.

다만 S8 작업 중 **연출 계층에서 두 가지 실제 결함**을 잡았고, 둘 다 E2E 가 잡았다.

| 발견 | 원인 | 조치 |
|---|---|---|
| 프레임 드랍 누적 2.3초 / 2초 | 전체 화면 오버레이를 `opacity: 0` 으로만 숨겨서, 투명해도 레이어가 매 프레임 합성되고 내부 무한 CSS 애니메이션이 계속 틱했다 | `visibility` 를 함께 전환하고, 애니메이션을 `.visible` 하위로 옮김 |
| 재시작 후 geometry 65 → 57 | 부팅 첫 프레임을 `HouseScene.update()` 전에 그려서, **아직 등장하지 않은 인간·특식 메시가 원점에 겹쳐 렌더**되고 그 지오메트리가 GPU 에 올라갔다 | 첫 프레임 렌더 전에 `update()` 를 먼저 돌림. 재시작 경로에도 `renderer.compile()` 추가 |

두 번째는 누수가 아니라 **첫 판이 과다 계상**된 것이라 R5 테스트가 "재시작 후 감소"로
실패했다. 화면상으로는 로딩 화면에 가려 보이지 않는 한 프레임이었지만,
그 프레임이 GPU 상태에 남는다는 점에서 실제 결함이 맞다.

### 3-8e. ⚠️ S7 인간 적 — 밸런스 미해결

§24 는 확장 기능이 밸런스에 영향을 주면 재검증하라고 요구한다. 재검증 결과
**인간 적을 넣으면 신중한 플레이가 클리어하지 못한다.**

| 구성 | 신중한 플레이 (5시드) |
|---|---|
| 인간 없음 (대조군) | **4/5 클리어** (5.4~6.9분, 피해 0) |
| 인간 있음 | **0/5** — 260~327초에 아사 |

원인은 추격 자체보다 **섭취 공백**이다. 배고픔 상한이 100 이라 몰아 먹은 잉여는
버려지는데, 인간을 피하는 동안 생기는 긴 공백에서 0 으로 떨어진다.

시도한 완화(전부 적용되어 있음):

| 조치 | 값 |
|---|---|
| 사냥/휴식 주기 | 8초 사냥 / 25초 휴식 |
| 추격 시간 상한 | 5초 (짧은 기습) |
| 시야 / 추적 해제 | 6.0 → 3.5 / 9.5 → 7.5 |
| 포기 시 행동 | 플레이어에게서 먼 곳으로 이동 |
| 배고픔 회복 | 10 → 13 |

그럼에도 봇 기준으로는 여전히 아사한다. **다만 이 봇은 쫓길 때마다 피난처로
가서 기다리는 과잉 대응을 한다** — 인간이 플레이어보다 느리므로(2.9 vs 3.2)
사람은 거리를 유지하며 계속 먹을 수 있다. 봇의 한계인지 실제 붕괴인지는
사람의 플레이로만 가릴 수 있다.

> **판단 보류.** MVP(우선순위 1~5)와 S6 은 게이트를 통과했고, 인간은 §24 확장
> 기능이다. `CONFIG.HUMAN_FROM_LEVEL` 을 99 로 두면 등장하지 않는다.

### 3-8d. 🚦 MVP 게이트 판정 (S5 완료)

`node tools/cycle-probe.ts` 가 **두 가지 플레이 성향**을 각각 5개 시드로 돌린다.
한쪽만 재면 "이길 수 있나"와 "긴장이 있나" 중 하나만 알 수 있다.

| 성향 | 사이클 | 도달(평균) | 도달 범위 | 평균 피해 |
|---|---|---|---|---|
| **신중** — 청소기가 가까우면 물러난다 | 16.15초 | 467초 (7.8분) | 6.6~9.2분 | **0.0** |
| **무모** — 청소기를 무시하고 영역만 채운다 | 14.26초 | ~~414초~~ | — | **3.0 (사망)** |

> 🔧 **정정 (S7).** 위 "무모한 플레이 414초"는 잘못된 수치였다. 당시 프로브가
> 하트 0 이후에도 시뮬레이션을 계속 돌려서, **죽은 뒤에 채운 영역까지 세고 있었다.**
> 사망 판정을 추가한 뒤 다시 재보니 무모한 플레이는 73~265초에 죽는다.
>
> 게이트 판정 자체는 그대로 유효하다 — 근거였던 **신중한 플레이는 피해 0 으로
> 진짜 클리어했고**, "무모하면 죽는다"는 결론은 오히려 더 분명해졌다.
> 그래도 잘못된 수치를 근거로 제시했던 것은 정정한다.

```
✅ 치트 없이 클리어 가능 (전 시드·전 성향)
✅ 신중하게 플레이하면 죽지 않고 클리어
✅ 무모하게 플레이하면 죽는다 — 청소기가 실제 위협
✅ 신중한 플레이 3/5 시드가 5~8분 구간
```

**이 표가 §1 의 "욕심 vs 안전" 긴장이 실제로 작동한다는 증거다.**
회피를 포기하면 **13% 빨라지지만 죽는다.** 둘 중 하나를 고르는 게 게임이 된다.
실제 플레이어는 두 봇 사이 어딘가이므로 약 7~7.5분이 될 것으로 본다.

> ⚠️ 신중한 플레이의 느린 쪽 2개 시드(8.9·9.2분)는 8분을 넘는다.
> 봇의 회피가 사람보다 과하기 때문이다(다가오지 않는 청소기도 피한다).
> §0-1 의 합격 기준은 기준 시나리오이므로 게이트는 통과로 본다.

### 3-8c. ✅ 최종 실측 — 로봇청소기 포함 (S4 완료)

MVP 의 모든 시스템이 붙은 상태에서 다시 측정했다. **§0-1 검증 완료.**

```
seed   사이클   배변  음식  도달(초)  도달(분)  최저 배고픔  받은 피해
1      14.49s   32   96    464       7.7       90          3
7      14.24s   28   84    399       6.6       89          3
42     14.02s   29   87    407       6.8       90          0
1337   14.39s   28   84    403       6.7       88          3
2024   14.16s   28   84    396       6.6       90          3
```

| 항목 | 모델 | 실측 | 판정 |
|---|---|---|---|
| 배변 사이클 | 13.41초 | 14.26초 | +6% |
| 44% 도달 | 399초 | **396~464초 (평균 414초)** | **+4%** ✅ |
| 5개 시드 전부 5~8분 구간 | — | 6.6~7.7분 | ✅ |

**모델이 실측을 4% 오차로 예측한다.** §0-1 의 계산이 실제 구현과 일치함을 확인했다.

> 🔎 **받은 피해 3 = 하트 0.** 봇은 청소기를 전혀 피하지 않으므로 5판 중 4판에서
> 하트가 바닥났다. 실제 게임이었다면 클리어 전에 GAME OVER 다.
> 이건 결함이 아니라 **의도한 긴장**이다 — 영역 경쟁은 회피를 안 해도 이길 수 있지만,
> 생존은 그렇지 않다. 청소기가 장식이 아니라는 증거다.

### 3-8b. 🔬 봇 플레이 실측 (S3 시점 — 청소기 없음)

§0-4 로 `systems/` 가 Three.js 를 쓰지 않으므로, **렌더러 없이 실제 게임 시스템을
그대로 돌려** 사이클을 측정할 수 있다. 이게 §0-4 를 지킨 실질적 이득이다.

```bash
node tools/cycle-probe.ts   # 봇이 5개 시드로 실제 플레이
```

봇은 "가장 가까운 음식으로 경로 탐색 → 먹기 → 게이지가 차면 미개척지로 → 배변"만
반복한다. 사람보다 낭비가 없으므로 결과는 **사이클 하한**이다.

| 항목 | 모델 | 봇 실측 | 차이 |
|---|---|---|---|
| 배변 사이클 | 13.41초 | **14.19초** | +6% |
| 44% 도달 (청소기 없음) | — | 324~401초 | — |
| 최저 배고픔 | — | 88~90 | 굶주림 피해 0 |

**모델이 실측을 6% 이내로 예측한다.** 초기 모델은 13.10초를 예상했으나 실측이
14.59초로 나와, 격자 경로의 우회 손실(`PATH_DETOUR` 1.12 → 1.25)과 리스폰 대기
(`RESPAWN_WAIT` 0.6 → 1.1)를 실측값으로 보정했다. 그 결과 도달 시간이 7.5분으로
상한에 붙어 `FOOD_MIN_SPAWN_DIST` 를 7.5 → 6.5 로 낮춰 6.7분으로 재중심화했다.

배고픔은 효율적으로 플레이하면 90 아래로 떨어지지 않는다. 의도한 설계다 —
D1 의 목적은 "굶겨 죽이기"가 아니라 **담요·가구 위 농성을 막는 것**이다.

> 이 측정에는 로봇청소기가 없었다. → **§3-8c 에서 청소기를 포함해 재측정 완료.**

### 3-9. 재검증 절차

계산식은 **`src/core/BalanceModel.ts` 한 곳**에 있고, `CONFIG` 를 직접 읽는다.
따라서 상수를 바꾸면 검증 결과가 자동으로 따라온다.

```bash
npm run balance   # tools/balance-check.ts — 위 표 전체를 재생산. 불합격이면 exit 1
npm test          # tests/balance.test.ts — 회귀를 CI 에 고정
```

`BLOCKED` 비율은 `furnitureLayout.ts` 에서 파생되며,
`CollisionMap.describe()` 실측값과 `ASSUMPTIONS.BLOCKED_RATIO` 가 5% 이상 벌어지면
`tests/collision.test.ts` 가 실패해 알려준다.

---

## 4. MVP 범위 (우선순위 1~5)

| # | 기능 | 포함 내용 |
|---|---|---|
| 1 | 이동과 카메라 | 8방향 이동, 달리기, OrthographicCamera 쿼터뷰, damping 추적, 경계 클램프, 가구 반투명 |
| 2 | 똥 땅 영역 확장 | 32×24 `Uint8Array` 격자, InstancedMesh 색상 갱신, 증분 달성률 |
| 3 | 배고픔·똥 게이지 | 슈퍼푸드 스폰/섭취, 게이지 충전·상한 폐기, 굶주림 유예·주기 피해 |
| 4 | 로봇청소기 | 읽히는 직선 이동 + 0.4초 회전 연출, 청소 판정, 충돌 피해·넉백·무적 |
| 5 | 승리·패배 | 44% 도달 시 `STAGE_CLEAR`, 하트 0 시 `GAME_OVER`, 결과 화면, `R` 재시작 |

**MVP 판정 게이트**: 5단계 완료 시점에 **치트 없이 한 판을 끝까지 클리어**하고 재미를 확인한 뒤에만 6단계로 진행한다.

## 5. 확장 기능 범위 (우선순위 6~8)

| # | 기능 |
|---|---|
| 6 | 담요 은신 (경고 → 강아지 이벤트), 화장실 이동 + 변기 보너스, 가구 등반 |
| 7 | Age/Lvl 성장, 인간 적 (Lvl 2~, 격자 A*, 0.5초 재계산 제한), 짝 도마뱀·임신, 특식 `SecretEvent` |
| 8 | 로딩·타이틀 연출, 프로시저럴 캐릭터 애니메이션, 표정, 파티클 풀, Web Audio 효과음, 튜토리얼 |

**보류(구현하지 않음)**: 랭킹, 상점, 도마뱀 카페, 온라인 기능 — 타이틀에 "열심히 싸는 중입니다" 안내만 노출.

---

## 6. 기술 구조

### 6-1. 원칙 반영 방식

| 원칙 | 구현 방법 |
|---|---|
| **§0-2 단일 진실 원천** | `world/furnitureLayout.ts`의 `FurnitureDef[]` 하나에서 ① `Furniture.ts`가 메시를, ② `CollisionMap.ts`가 AABB를, ③ `TerritorySystem`이 `BLOCKED` 셀을 **파생**. 손으로 중복 기입 금지 |
| **§0-3 물리 엔진 금지** | `CollisionMap.ts`에 원-AABB / 원-원 판정만 직접 구현 (~150줄). 외부 물리 라이브러리 의존성 0 |
| **§0-4 로직/렌더 분리** | `core/`·`systems/`·`world/CollisionMap.ts`는 Three.js를 **import 하지 않는다.** 자체 `Vec2 { x, z }` 사용. `entities/`·`scenes/`는 상태를 읽어 메시에 반영만 하는 **단방향** |
| **§0-5 결정성** | `GameLoop.ts` accumulator 고정 1/60초(최대 5스텝 캐치업). `Rng.ts` mulberry32를 `GameState`에 주입, `Math.random()` 전역 금지 (ESLint 규칙으로 강제) |
| **§0-6 오디오 언락** | `SoundManager.unlock()`을 타이틀 첫 입력 핸들러에서 1회 호출. 그 전 재생 요청은 무시 |

### 6-2. 데이터 흐름

```
InputManager ──┐
               ├──> GameState (순수 데이터) ──> systems/* (순수 함수, 상태 변이)
Rng ───────────┘                                      │
                                                      ↓ (읽기 전용)
                                          entities/* + scenes/* (Three.js)
                                          ui/* (HTML 오버레이)
```

시스템 간 통신은 `EventBus`로 하고, 렌더/사운드는 이벤트를 **구독만** 한다.
로직이 렌더를 직접 호출하는 경로를 만들지 않는다.

### 6-3. 검증 가능성 설계

- `systems/`가 순수하므로 **헤드리스 시뮬레이션**을 만들 수 있다.
  `tools/balance-check.mjs`가 실제 시스템 코드를 가져다 봇 플레이를 돌려
  §3의 예측과 실제 구현이 일치하는지 확인한다. 이것이 밸런스의 **최종 진실**이다.
- 개발 모드에서 `window.__GAME__ = { state, debug }` 노출 → Playwright가 내부 상태를 직접 검증.

---

## 7. 주요 구현 위험 요소

| # | 위험 | 영향 | 대응 |
|---|---|---|---|
| R1 | **BLOCKED 비율이 12%를 벗어남** | §3 계산 전체가 무너짐 | 격자 생성 직후 실측 비율을 콘솔 출력. 10~15% 밖이면 배치 조정. 테스트로 assert |
| R2 | **실제 배변 사이클이 예측(13.1초)과 다름** | 도달 시간이 5~8분을 벗어남 | 디버그 패널에 실시간 `G`/`S·p`/순증가율 계측 표시. 4단계 완료 즉시 실측 후 상수 재조정 |
| R3 | 가구 렌더/충돌/BLOCKED 불일치 | "보이지 않는 벽" 버그 | §0-2 단일 원천 강제. 디버그 모드에서 AABB·격자 오버레이 동시 표시 |
| R4 | 격자 색상 갱신이 프레임을 잡아먹음 | FPS 저하 | InstancedMesh 1개 + `instanceColor` 부분 갱신. **변경된 셀이 있는 프레임에만** `needsUpdate` |
| R5 | 재시작 시 GPU 리소스 누수 | 3회 재시작 후 크래시 | `dispose()` 일괄 처리 헬퍼. `renderer.info.memory` 카운트를 테스트로 비교 |
| R6 | 청소기 이동이 무작위처럼 보임 | 회피 학습 불가 → 불공정하게 느껴짐 | 직선 1.5~3.5초 유지 + 0.4초 가시적 회전 + 진행 방향 바닥 데칼 |
| R7 | 화장실 체류 중 거실 시뮬레이션 정지 | 변기 보너스의 리스크가 사라짐 | 화장실은 **씬 전환이 아니라 좌표 이동**. 거실 로직은 계속 갱신 |
| R8 | 배고픔이 사실상 무의미 | 게이지 하나가 장식이 됨 | `FOOD_HUNGER_RESTORE` 25 → 10 (아래 **D1**) |
| R9 | Lvl 3 성장이 밸런스를 가속 | 후반이 너무 쉬워짐 | 반경 증가폭을 원안 대비 축소(2.3/2.5/2.7). 히트박스 확대로 리스크 상쇄 |

### ✅ 확정된 설계 결정 (2026-08-07 승인)

**D1. `FOOD_HUNGER_RESTORE` 25 → 10.**
원안(25)은 음식 3개/사이클 × 25 = **사이클당 +75** vs 소모 13.1 × 1.5 = **18.3** 이라
배고픔이 항상 가득 차 사실상 장식이 된다.
10으로 낮추면 사이클당 **+30 vs 소모 18.3** — 효율적 플레이는 여유가 있고,
헤매거나 담요·가구 위에서 농성하면 실제로 굶는다. 게이지가 제 역할을 하게 된다.
§9-2의 "25 회복" 명세를 대체하며, **영역 모델(`G₀`/`S`/`p*`)에는 영향이 없어 재검증 불필요.**

**D2. §3-7의 상수 조정안 전체 승인.**
`POOP_PER_FOOD 50 → 34`(음식 2개 → 3개) 포함. §4 주석의 "음식 2개 = 1회 배변"을 대체한다.
목표 44%와 `VACUUM_SPEED_CELLS 1.2`는 원안대로 유지.

---

## 8. 단계별 작업 순서와 완료 조건

| 단계 | 작업 | 완료 조건 |
|---|---|---|
| **S0** | Vite + TS(strict) + Vitest + Playwright 스캐폴딩, `GameConfig`, `Rng`, `GameLoop`, `EventBus` | `npm run build` 통과. 고정 타임스텝 단위 테스트 통과. 시드 재현성 테스트 통과 |
| **S1** | `furnitureLayout` → 메시/AABB/BLOCKED 파생, 거실 렌더, 쿼터뷰 카메라, 도마뱀(박스 3개) 이동 | 벽·가구 통과 불가. **BLOCKED 실측 10~15% 콘솔 출력.** 창 리사이즈 정상 |
| **S2** | 격자 `Uint8Array`, InstancedMesh 시각화, 배변 → 셀 전환, 달성률 증분 계산, HUD | Space로 초록 영역 확장. HUD 달성률 실시간 갱신. 달성률 단위 테스트 통과 |
| **S3** | 슈퍼푸드 스폰/섭취, 배고픔·똥 게이지, 굶주림 피해, Age 카운트 | 30초 안에 첫 배변 가능. 게이지 상한 폐기·초기화 테스트 통과 |
| **S4** | 로봇청소기 (읽히는 이동, 청소, 충돌·넉백·무적) | 달성률이 실시간 감소. 연속 충돌로 즉사하지 않음. **디버그 계측으로 `G`/`S·p` 실측** |
| **S5** | 승패 판정, 결과 화면, `R` 재시작, 리소스 정리 | **치트 없이 한 판 클리어.** 3회 재시작 후 `renderer.info.memory` 동일 |
| **🚦 게이트** | **MVP 플레이 검증** | 실제로 재미있는가? 도달 시간이 5~8분인가? 아니면 상수 재조정 후 S4로 복귀 |
| **S6** | 담요 은신, 화장실 + 변기 보너스, 가구 등반 | 세 상황 모두에서 `Space` 차단. 변기 BFS 확장이 고립 셀을 만들지 않음 |
| **S7** | Age/Lvl 성장, 인간 적, 짝 도마뱀, 특식 | Lvl 2 전환 시 인간 등장. 은신·등반으로 추적 해제 |
| **S8** ✅ | 로딩·타이틀, 캐릭터 애니메이션·표정, 파티클, 사운드, 튜토리얼, 디버그 패널 | 오디오 언락 동작. 프로덕션 번들에 디버그 UI 미포함 — **둘 다 E2E 로 확인** (§3-8f) |
| **S9** ✅ | 테스트 정비, 성능 최적화, README | 단위 229개 + E2E 26개 **전체 통과**(3회 연속). 콘솔 에러 0 — 개발 서버와 **프로덕션 빌드 양쪽**에서 (§3-8g) |

---

## 9. 예상 파일 구조

```
geko-house3/
├─ ROADMAP.md
├─ README.md
├─ index.html
├─ package.json / tsconfig.json / vite.config.ts
├─ vitest.config.ts / playwright.config.ts
├─ tools/
│  └─ balance-check.mjs        // §3 재검증 (npm run balance)
├─ tests/                       // Vitest — Three.js 없이 실행
│  ├─ territory.test.ts
│  ├─ hunger-poop.test.ts
│  ├─ vacuum.test.ts
│  ├─ damage.test.ts
│  ├─ growth.test.ts
│  ├─ determinism.test.ts
│  ├─ restart.test.ts
│  └─ balance.test.ts          // §21 밸런스 회귀 (p* > 0.44, 300~480초)
├─ e2e/
│  ├─ helpers.ts               // 시드 고정 부팅 · 게임 시간 대기 (S9)
│  ├─ smoke.spec.ts            // 개발 서버 (내부 상태 검증)
│  └─ production.spec.ts       // 빌드 산출물 (§19 디버그 UI 부재 검증)
└─ src/
   ├─ main.ts
   ├─ core/      Game / GameLoop / GameState / GameConfig / Rng / InputManager / EventBus
   ├─ scenes/    HouseScene / QuarterViewCamera
   ├─ entities/  Gecko / RobotVacuum / Human / Dog / Food / ParticlePool  (읽기 전용)
   ├─ systems/   Territory / Hunger / Poop / Damage / Spawn / Vacuum / Interaction  (순수)
   ├─ world/     furnitureLayout / LivingRoom / Bathroom / Furniture / CollisionMap
   ├─ ui/        HUD / ResultScreen / LoadingScreen / TitleScreen / Tutorial / Prefs / DebugPanel
   ├─ audio/     SoundManager
   └─ assets/
```

> **S8 에서 갈라진 부분.** 위 표는 로딩·타이틀을 `scenes/` 에 두었지만, 실제로는
> `ui/` 의 HTML 오버레이로 만들었다. 둘 다 배경으로 **HouseScene 을 그대로 렌더**하기
> 때문이다 — 타이틀 뒤에서 실제 거실이 돌아가는 편이 정지 화면보다 게임을 잘 설명하고,
> 별도 Three.js 씬을 하나 더 만들면 §8 의 dispose 대상만 늘어난다.
> `assets/` 는 만들지 않았다. 메시도 소리도 전부 코드로 생성해서 받아올 파일이 없다.

`systems/`와 `core/`에는 Three.js import를 금지하는 ESLint 규칙(`no-restricted-imports`)을 건다.

---

## 10. Git 커밋 계획

| # | 커밋 | 단계 |
|---|---|---|
| 1 | `chore: initialize three.js game project` | S0 |
| 2 | `docs: add development roadmap with balance verification` | S0 |
| 3 | `feat: add fixed-timestep game loop and state machine` | S0 |
| 4 | `feat: add seeded rng and game config` | S0 |
| 5 | `feat: create quarter-view house scene from furniture layout` | S1 |
| 6 | `feat: add collision map derived from furniture layout` | S1 |
| 7 | `feat: add gecko movement and camera follow` | S1 |
| 8 | `feat: implement poop territory grid with instanced rendering` | S2 |
| 9 | `feat: implement HUD and character stats` | S2 |
| 10 | `feat: add food and poop interaction loop` | S3 |
| 11 | `feat: add robot vacuum enemy with readable movement` | S4 |
| 12 | `feat: add damage, invulnerability and game over flow` | S4 |
| 13 | `feat: add stage clear condition and result screen` | S5 |
| 14 | `test: add core gameplay and balance regression tests` | S5 |
| 15 | `feat: add blanket hiding mechanic` | S6 |
| 16 | `feat: add bathroom and toilet bonus` | S6 |
| 17 | `feat: add climbable furniture` | S6 |
| 18 | `feat: add age and level growth system` | S7 |
| 19 | `feat: add human enemy for level two` | S7 |
| 20 | `feat: add title and loading screens with audio unlock` | S8 |
| 21 | `feat: add debug panel with balance metrics` | S8 |
| 22 | `test: add playwright smoke test` | S8 |
| — | *실제로는 20~22 를 `feat: add presentation layer` 한 커밋으로 합쳤다. 사운드·파티클·표정·튜토리얼이 서로를 참조해 쪼개면 중간 커밋이 빌드는 되어도 화면이 반쯤 비게 된다.* | S8 |
| 23 | `refactor: optimize rendering and object lifecycle` | S9 |
| 24 | `docs: add setup and gameplay instructions` | S9 |
| — | *실제로는 23~24 대신 `fix: make runs reproducible and measure game time in tests` 로 갔다. S9 에서 실제로 필요했던 건 렌더 최적화가 아니라 **재현성**이었다 — 시드 고정과 `droppedTime` 리셋이 없으면 성능을 재도 그 수치를 믿을 수 없다.* | S9 |

각 커밋은 `npm run build`(`tsc --noEmit` 포함)가 통과하는 실행 가능 상태를 유지한다.
완료되지 않은 기능은 커밋 메시지에 적지 않는다.

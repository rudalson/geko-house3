# 🦎 게코 하우스 서바이벌 (Gecko House Survival)

> 음식을 먹고 똥 게이지를 채운 뒤, 로봇청소기를 피해
> **거실 바닥의 44%를 '똥 땅'으로 만드는** 1인칭 3D 생존·영역 확장 게임.

화면은 **바닥 34cm, 게코의 눈높이**다. 의자 다리가 기둥처럼 서 있고 강아지는
올려다봐야 하며, 로봇청소기는 지평선에서 커지는 덩어리로 다가온다.
방 전체를 보는 유일한 수단은 우측 상단의 **미니맵**이다.

플레이어는 점(點)으로 칠하고, 로봇청소기는 선(線)으로 지운다.
바닥의 갈색 똥 땅이 실시간으로 늘고 줄어드는 것 자체가 게임의 피드백이다.

---

## 실행

```bash
npm install
npm run dev        # http://localhost:5173
```

| 명령 | 설명 |
|---|---|
| `npm run dev` | 개발 서버 |
| `npm run build` | 타입 검사(`tsc --noEmit`) + 프로덕션 빌드 |
| `npm run preview` | 빌드 결과 미리보기 |
| `npm test` | 단위 테스트 (Vitest) |
| `npm run test:e2e` | 스모크 테스트 (Playwright) — 개발 서버 23개 + **프로덕션 빌드 3개** |
| `npm run balance` | **밸런스 검증** — 불합격이면 exit 1 |
| `node tools/cycle-probe.ts` | 봇 플레이로 실제 사이클·도달 시간 실측 |

E2E 를 처음 돌리기 전에 브라우저를 한 번 받아야 한다: `npx playwright install chromium`

`test:e2e` 는 서버를 두 개 띄운다 — 개발 서버(5173)와 **실제 빌드 산출물**(4173).
후자는 `npm run build` 를 포함하므로 첫 실행이 조금 오래 걸린다.
프로덕션 쪽만 돌리려면 `npx playwright test --project=production`.

같은 판을 다시 보고 싶으면 주소에 시드를 붙인다: `http://localhost:5173/?seed=1337`.
재시작해도 그 시드에서 파생되므로 세션 전체가 재현된다. (§0-5)

---

## 조작

| 키 | 동작 |
|---|---|
| `W` `S` / `↑` `↓` | 앞으로 · 뒤로 (뒤는 55% 속도 — 1인칭에서 뒤는 안 보인다) |
| `A` `D` / `←` `→` | 몸 돌리기 (제자리 선회 · 206°/초) |
| `Shift` | 짧은 달리기 (1.5초 지속 / 3초 쿨다운) |
| `E` 또는 `Z` | 상황별 상호작용 — 먹기 / 담요에 숨기 / 가구 오르내리기 / 화장실 / 변기 / **짝** |
| `Space` | 똥 싸기 |
| `Esc` | 일시정지 |
| `M` | 음소거 |
| `R` | 클리어·게임오버 후 재시작 |
| `` ` `` | 디버그 패널 (**개발 빌드 전용**) |

실행하면 로딩을 거쳐 타이틀 화면에서 멈춘다. 아무 키나 누르면 시작하고,
그 첫 입력에서 오디오가 언락된다. 타이틀에서 튜토리얼과 효과음을 끌 수 있고,
설정은 `localStorage` 에 남는다.

---

## 게임 규칙

1. **슈퍼푸드 3개**를 먹으면 똥 게이지가 가득 찬다.
2. 게이지가 찬 상태에서 `Space` — **1초 동안 움직일 수 없고 무적도 아니다.**
3. 배변 지점 반경이 갈색 '똥 땅'이 된다. **이미 확보한 곳에 겹쳐 싸면 손해다.**
4. 로봇청소기가 돌아다니며 똥 땅을 지운다. 부딪히면 하트가 1개 준다.
5. 거실 유효 바닥의 **44%** 를 확보하면 승리. 하트 3개를 모두 잃으면 패배.

**핵심 트레이드오프**

- 게이지가 가득 찬 상태에서 음식을 먹으면 초과분이 버려진다 → "먼저 쌀지, 더 먹을지"
- 성장하면 배변 반경이 커지지만 **히트박스도 커진다** → 강해질수록 눈에 띈다
- 넓은 미개척지 한복판이 가장 많이 벌지만 가장 위험하다

**피난처는 공짜가 아니다**

| | 얻는 것 | 잃는 것 |
|---|---|---|
| 담요 밑 (`E`) | 청소기·인간 판정 제외 | 6초 뒤 경고, 4초 더 있으면 강아지에게 하트 -1 |
| 가구 위 (`E`) | 판정 제외 + 시야 확보 | **배변 불가** — 그동안 영역은 계속 깎인다 |
| 화장실 (`E`) | 변기 한 방에 34칸 + 청소기 8초 감속 | 왕복 20여 초, **그동안에도 거실은 계속 청소된다** |

셋 다 배고픔은 계속 줄어든다. 안전한 곳에서 버티는 전략은 성립하지 않는다.

---

## 기술 스택

TypeScript(`strict`) · Vite · Three.js · Vitest · Playwright.
React 를 쓰지 않는다. **외부 물리 엔진도 쓰지 않는다** — 필요한 건 원-AABB 와
원-원 판정뿐이라 `world/CollisionMap.ts` 에 직접 구현했다.

### 설계 원칙

| 원칙 | 구현 |
|---|---|
| **가구 배치는 한 곳에만** | `world/furnitureLayout.ts` 하나에서 ① 렌더 메시 ② 충돌 AABB ③ 격자 `BLOCKED` 셀이 모두 파생된다 |
| **게임 로직은 Three.js 를 모른다** | `core/`·`systems/` 는 순수 TypeScript. 덕분에 **렌더러 없이 실제 시스템으로 밸런스를 실측**할 수 있다 (`tools/cycle-probe.ts`) |
| **결정적 시뮬레이션** | 고정 1/60초 타임스텝 + 시드 기반 PRNG. 같은 시드는 같은 판을 만든다 |
| **밸런스 계산식은 한 곳에만** | `core/BalanceModel.ts` 가 `GameConfig` 를 직접 읽는다. 상수를 바꾸면 검증 결과가 자동으로 따라온다 |

`tests/architecture.test.ts` 가 이 규칙들을 코드로 강제한다 —
순수 계층의 Three.js import, `Math.random()` 직접 호출, `CONFIG` 중복 정의를 막는다.

---

## 폴더 구조

```
src/
├─ core/        GameConfig · BalanceModel · GameState · GameLoop · Rng · InputManager · EventBus · Game
├─ systems/     Territory · Poop · Hunger · Damage · Spawn · Vacuum · Interaction
│               Human · Treat(SecretEvent)
│               Movement · Pathfinding · Shelter(은신·등반·화장실)
├─ world/       furnitureLayout·bathroomLayout(단일 원천) · CollisionMap
│               LivingRoom · Bathroom · Furniture · Decor
│               modelKit·kitFurniture·kitProps·furnitureModels (Kenney 키트)
├─ entities/    Gecko · RobotVacuum · Human(+humanAvatar·humanBody) · Food · Treat
│               TerritoryGrid · ParticlePool                    (읽어 그리기만)
├─ scenes/      HouseScene · QuarterViewCamera
├─ audio/       SoundManager                     (EventBus 를 구독만 한다)
└─ ui/          HUD · ResultScreen · LoadingScreen · TitleScreen
                Tutorial · Prefs · DebugPanel(+css, DEV 전용)    (HTML 오버레이)

public/models/  Kenney 에셋에서 골라 오거나 구운 .glb·.png (CC0, CREDITS.txt 참고)

tools/          balance-check.ts(검증 리포트) · cycle-probe.ts(봇 실측)
                convert-character.mjs(FBX → human.glb, `npm run bake:character`)
tests/          Vitest — Three.js 없이 실행
e2e/            Playwright 스모크
```

---

## 구현된 기능 (MVP)

- [x] 1인칭 3D 거실 (눈높이 0.34, 사방 벽 + 천장), 가구 충돌
- [x] 탱크 조작 (전진·후진 + 제자리 선회) + 짧은 달리기
- [x] 우측 상단 미니맵 — 거실·화장실·가구·똥 땅·적·내 시선
- [x] 32×24 똥 땅 격자, InstancedMesh 시각화, 실시간 달성률
- [x] 슈퍼푸드 스폰·섭취, 배고픔, 똥 게이지, Age/Lvl 성장
- [x] 로봇청소기 (읽히는 이동 · 청소 · 충돌 피해 · 넉백 · 무적)
- [x] 승리/패배 판정, 결과 화면, `R` 재시작, `Esc` 일시정지
- [x] HTML/CSS HUD
- [x] 담요 은신 (6초 경고 → 강아지 이벤트)
- [x] 화장실·변기 보너스 (BFS 덩어리 확장 + 청소기 감속, 체류 중 거실은 계속 청소됨)
- [x] 가구 등반 (상판 자유 이동, 청소기 판정 제외, 배변 불가)
- [x] 인간 적 (Lvl 2 등장, 격자 BFS 추적) — ⚠️ 밸런스 미해결, "알려진 문제" 참조
- [x] 특식과 SecretEvent 6종 (`SECRET_EVENTS` 배열에 추가만 하면 확장)
- [x] 짝 도마뱀·임신·산란 — 아래 "짝 도마뱀·임신" 절 참조

## 구현된 기능 (S8 — 연출·UX)

- [x] 로딩 화면 — 셰이더 컴파일·밸런스 검산·첫 프레임을 **실제로** 진행률에 반영
- [x] 타이틀 화면 (뒤에서 거실이 계속 렌더된다) + **첫 입력에서 오디오 언락** (§0-6)
- [x] Web Audio 효과음 — 에셋 없이 오실레이터·노이즈로 합성.
      청소기 험은 **거리에 따라 커진다** (화면 밖 위협을 귀로 안다)
- [x] 파티클 풀 — InstancedMesh 1개, 배변·먹기·피격·레벨업·특식·청소 먼지
- [x] 도마뱀 표정 — 입·눈꺼풀 추가, 배고픔/위협/힘주기/씹기/피격을 얼굴로 알린다.
      겁먹으면 위협 쪽을 본다
- [x] 단계별 튜토리얼 — 별도 스테이지 없이 실제 판 위에서 조건 충족 시 자동 진행
- [x] 디버그 패널 (`` ` ``) — `G` / `S·p` / 순증가율 / 실측 사이클 (R2 대응).
      **프로덕션 번들에서 스크립트·CSS 모두 제외됨** (동적 import + `import.meta.env.DEV`)

## S9 — 테스트 정비·성능

E2E 가 개발 PC 에서 간헐적으로 깨지던 문제를 **임계값을 낮추지 않고** 해결했다.
원인은 네 가지였고, 둘은 제품 코드의 결함이었다.

| 원인 | 실제로 무슨 일이 났나 | 조치 |
|---|---|---|
| **시드가 `Date.now()`** | §0-5 는 "같은 시드면 같은 판"을 요구하는데 밖에서 시드를 줄 방법이 없었다. 실행마다 음식 배치가 달라져, 가구 옆에서 `E` 를 누르는 테스트가 하필 그 자리에 음식이 스폰된 실행에서만 깨졌다 — 재현이 안 되니 원인도 못 찾는다 | `?seed=1337` 지원. 재시작도 그 시드에서 파생돼 세션 전체가 재현된다 |
| **`droppedTime` 이 리셋되지 않음** | 로딩·타이틀 구간의 히치(셰이더 컴파일·첫 렌더)가 계속 누적돼, 부팅에서 한 번 튄 게 그대로 "플레이 중 성능 문제"로 보고됐다. §19 패널의 값도 재시작하면 의미가 없었다 | `GameLoop.resetStats()` 를 판 시작·재시작에서 호출 |
| **벽시계로 게임 시간을 잼** | 배변 애니메이션 1초는 게임 시간인데 테스트는 벽시계 3초를 기다렸다. 프레임이 밀리면 게임 시간 1초가 벽시계 3초를 넘는다 | `expectWithinGameTime` — 예산은 게임 시간, 벽시계는 안전망 |
| **`E` 의 대상을 가정함** | 음식은 사정거리 안이면 등반·화장실보다 **항상 우선**이다. 테스트는 그걸 모른 채 눌렀다 | `debug.interaction()` 으로 무엇이 걸렸는지 확인 후 누른다 (`pressInteract`) |

그 밖에:

- **소크 테스트가 실은 게임을 돌리지 않고 있었다.** 무작위 보행은 시작점 주변만
  맴돌아 음식(6.5 units 밖 스폰)에 한 번도 닿지 못했다. 이전에는 `Math.random()`
  의 운으로 가끔 통과했을 뿐이다. 가장 가까운 음식을 향해 걷도록 고치니
  **플레이 중 지오메트리가 는다**는 별개의 신호가 드러났다.
- 그 신호는 누수가 아니었다. `renderer.info.memory` 는 메시가 **처음 그려질 때**
  올라가는 것까지 세므로, Lvl 2 에서 인간이 등장하면 아무것도 새로 만들지 않았는데
  57 → 59 → 62 로 오른다. 씬 그래프가 참조하는 리소스 수를 직접 세는
  `debug.sceneStats()` 를 추가해 착시를 없앴다. (R5)
- **프로덕션 빌드 E2E 를 추가했다.** S8 은 "디버그 UI 가 번들에 없다"를 문자열
  grep 으로만 확인했는데, 이름이 사라져도 코드는 남을 수 있다. 실제 빌드 산출물을
  띄워 `` ` `` 를 눌러도 패널이 열리지 않고 `window.__GAME__` 이 없음을 확인한다.
- Three.js 를 별도 청크로 분리 (583 kB 한 덩어리 → 87 kB + 496 kB).
- `climbableFurniture()` 가 매 렌더 프레임 배열을 새로 만들던 것을 상수화.

### 도마뱀이 뒷걸음질치던 문제

플레이 중 보고된 버그다. **모델은 머리를 로컬 −Z 에 두고 만들었는데**
(`head.position.z = -0.36`, 꼬리 `+0.36`), `facing = atan2(dirX, dirZ)` 는 로컬
**+Z** 를 진행 방향에 맞추는 값이다. 그래서 꼬리가 앞장서서 180° 반대로 걸었다.

고치면서 같은 회전 규약을 쓰는 `threatSide()` 의 역회전 부호도 뒤집혀 있던 것이
드러났다. 정면을 보고 있을 때 **오른쪽 위협을 왼쪽이라고 답해서** 겁먹은
눈동자가 반대로 돌아가고 있었다.

1인칭으로 바꾼 뒤 이 규약의 값어치가 오히려 올라갔다. 도마뱀 모델은 이제
화면에 나오지 않지만 **카메라가 같은 규약을 그대로 쓴다** — 부호가 뒤집히면
`W` 를 눌렀을 때 화면이 뒷걸음질치는, 게임을 못 하는 버그가 된다.
`tests/gecko-facing.test.ts` 가 8방향 전부에 대해 모델의 머리 위치와
**카메라의 시선 벡터를 함께** 검사한다. 화면을 보지 않고 잡을 수 있어야
다시 어긋나지 않는다 (Three.js 의 Object3D 계산은 WebGL 없이 node 에서 돈다).

## 1인칭 게코 시점 (§25)

쿼터뷰를 걷어내고 카메라를 도마뱀 눈높이(**0.34 world units**)에 내렸다.
같은 방, 같은 가구, 같은 밸런스인데 인상이 통째로 바뀐다 — 16×12m 거실이
협곡이 되고, 높이 0.8 짜리 안락의자가 올려다봐야 하는 절벽이 된다.

시점 하나를 바꾸자 **여섯 군데가 함께 뒤집혔다.** 이게 이 작업의 실제 크기다.

| 무엇이 | 쿼터뷰에서는 | 1인칭에서는 |
|---|---|---|
| 조작 | 8방향 — 방향 전환 비용 0 | **탱크 조작** — 선회에 시간이 든다 |
| 가구 | 가리면 반투명 (`updateOcclusion`) | 가리는 게 정상. 대신 **눈이 파묻힌 소품만** 걷어낸다 (`updateNearFade`) |
| 벽 | 남·동쪽은 일부러 뚫어 뒀다 (캐릭터가 가려지니까) | 그쪽을 쳐다보게 된다 — **사방 벽 + 천장** |
| 등반 힌트 | 가구 **상판** 테두리 | 눈높이에서는 안 보인다 → **옆면 아래쪽** 띠 |
| 도마뱀 모델 | 화면의 주인공 | 카메라가 머리 안에 있어 통째로 감춘다. 대신 `GeckoSnout` 이 **자기 주둥이**를 화면 아래에 건다 |
| 방 전체 파악 | 화면이 공짜로 준다 | **미니맵**이 아니면 알 방법이 없다 |

마지막 줄이 핵심이다. 미니맵은 장식이 아니라 **쿼터뷰가 공짜로 주던 정보를
옮겨 담은 자리**다. 청소기 위치·미개척 바닥·화장실 문 방향이 전부 거기서만
읽힌다. 그래서 지도에 **방향을 가진 표시(내 시선 삼각형)** 가 반드시 필요하다 —
지도에서 청소기를 찾아도 지금 시선 기준으로 왼쪽인지 뒤인지 모르면 어느 키를
누를지 알 수 없기 때문이다.

### 선회 시간은 밸런스 상수다

`TURN_SPEED` 를 `GameConfig` 에 둔 이유다. 8방향 시절에는 목표를 바꿀 때
공짜로 방향을 틀 수 있었지만, 이제는 **선회 시간이 배변 사이클에 그대로
얹힌다.** 봇 실측으로 그 대가를 쟀다 — 자세한 수치는 ROADMAP §3-8k.

## 짝 도마뱀·임신 (§24)

**변기와 같은 축의 선택지**다 — 지금 손해를 보고 나중에 덩어리로 받는다.
대가의 성격만 다르다.

| | 변기 | 짝 |
|---|---|---|
| 대가 | 자리를 비운다. 안전하지만 아무것도 못 한다 | 자리를 안 비운다. 대신 **느려지고(×0.78) 커진 채로(×1.25) 25초를 버틴다** |
| 무방비 | 없음 | 교미 2.5초 — 배변과 같은 규칙 |
| 보상 | 34칸 | 24칸 |
| 이득비 | 1.10x / 1.29x | **1.24x / 1.43x** |

둘이 서로를 대체하지 않는다. 청소기가 멀면 변기를 다녀오고, 가까우면 짝을
미루게 된다. 짝을 조금 더 유리하게 둔 이유는 셀 계산에 안 잡히는 대가가
이쪽에만 있어서다 — 교미하는 2.5초 동안 무방비이고, 임신 내내 더 잘 걸린다.

흐름: 45초 뒤 등장 → `E` 로 교미(2.5초 정지) → 임신 25초 → 산란 24칸 →
75초 쿨다운 뒤 재등장. 임신 중에도 **배변은 막히지 않는다** — 25초 내내 못 싸면
대가가 너무 커서 아무도 짝에게 가지 않는다.

밸런스 근거와 재검증은 [`ROADMAP.md`](./ROADMAP.md) §3-8h.
`?seed=` 로 판을 고정하면 짝이 나타나는 자리도 재현된다.

> ⚠️ **봇 실측으로는 이득을 확인하지 못했다.** `cycle-probe` 에 짝 활용 성향을
> 추가했지만, 인간 적이 켜져 있으면 신중한 플레이가 아래 "알려진 문제" 대로
> 5시드 전부 아사해서 도달 시간이 안 나온다. 대조군과 사망 시각이 사실상
> 같다는 것(= 짝이 그 문제를 악화시키지 않는다)까지만 확인했다.

## 아직 구현되지 않은 기능

로드맵 S0~S9 의 계획 기능은 전부 구현됐다. 남은 것은 기능이 아니라
**판단이 필요한 밸런스 한 건**이다 (아래 "알려진 문제" 첫 항목).

---

## 밸런스를 바꾸려면

모든 밸런스 상수는 **`src/core/GameConfig.ts` 한 곳**에 있다.

```bash
# 1. GameConfig.ts 수정
# 2. 해석·수치 모델로 재검증 (불합격이면 exit 1)
npm run balance
# 3. 실제 시스템으로 봇 플레이 실측
node tools/cycle-probe.ts
# 4. 회귀 테스트
npm test
```

`tests/balance.test.ts` 가 **평형 점유율 > 0.44** 와 **44% 도달 300~480초**를 강제한다.
이 테스트가 깨지면 상수를 되돌리거나, 의도한 변경이라면 `ROADMAP.md` §3 의 표를 갱신한다.

가구를 옮기면 `BLOCKED` 비율이 바뀐다. 허용 범위(10~15%)를 벗어나면
`tests/collision.test.ts` 가 실패한다. 실측값은 게임 시작 시 콘솔에도 출력된다.

자세한 검증 근거는 [`ROADMAP.md`](./ROADMAP.md) §3 참조.

---

## 에셋

Kenney 의 CC0 에셋 두 벌을 쓴다. 둘 다 원본 배포판은 `.gitignore` 하고,
게임이 실제로 받는 파일만 `public/models/` 에 커밋한다 (합쳐서 약 650 kB).

| 에셋 | 쓰는 곳 | 커밋하는 것 |
|---|---|---|
| [Furniture Kit](https://kenney.nl/assets/furniture-kit) | 거실·화장실 가구와 장식 | `*.glb` 24개 (310 kB) |
| [Animated Characters 2](https://kenney.nl/assets/animated-characters-2) | 인간 적 (§24) | `human.glb` (215 kB) + 얼굴 3장 (114 kB) |

그 밖의 것 — 도마뱀·청소기·파티클·똥 땅 텍스처·말풍선·소리 — 은 전부 코드로 만든다.

### 인간은 FBX 를 **미리 구워** 쓴다

캐릭터 원본은 FBX 이고 애니메이션이 모델과 다른 파일에 있다. 런타임에 읽으려면
`FBXLoader`(110 kB)를 번들에 넣고 2.3 MB 를 받아야 하므로, 빌드 타임에 GLB 하나로
굽는다. 이미 번들에 있는 `GLTFLoader` 로 파일 하나만 받으면 된다.

```bash
npm run bake:character      # tools/convert-character.mjs → public/models/human.glb
```

굽는 김에 쓰지 않는 것을 덜어낸다 — 흰색뿐인 정점 색, 더미 클립, 값이 변하지 않는
position·scale 트랙, 중복 정점(4812 → 1029). 얼굴 스킨은 GLB 에 **넣지 않는다.**
판마다 다른 사람이 와야 해서(§24) 런타임에 갈아끼우기 때문이다.

모델을 그대로 씬에 붙이지 않는다. `world/modelKit.ts` 가 glTF 머티리얼의 **색만
정점에 구워** 가구 하나를 지오메트리 한 덩어리로 합친다. 덕분에 가구당 draw call 이
하나로 유지되고, 가림 페이드(§25)와 `dispose`(§8)가 예전 코드 그대로 동작한다.

| 파일 | 역할 |
|---|---|
| `world/modelKit.ts` | GLB 로드·캐시, 목표 상자에 맞춰 늘리고 색 입히기 |
| `world/furnitureModels.ts` | 가구 id → 어떤 모델을 어디에 놓을지 (순수 데이터) |
| `world/kitFurniture.ts` | 위 둘을 합쳐 `FurnitureDef` → 메시로 |
| `world/kitProps.ts` · `world/Decor.ts` | 충돌 없는 장식 (벽등·천장등·욕조·거울) |
| `entities/humanAvatar.ts` | 인간의 몸 — 스킨드 모델(얼굴 3종·idle/run) 또는 예전 조립 |
| `entities/humanBody.ts` | 코드로 만든 예전 로우폴리 사람 (폴백) |

모델이 하나라도 없으면(404·오프라인) 그 가구는 `world/furnitureBuilders.ts` 의,
사람은 `entities/humanBody.ts` 의 예전 조립으로 떨어진다. 에셋 때문에 게임을
못 켜는 일은 없다. 사람은 특히 장식이 아니라 **적**이라 (§24) 화면에서 사라지면
왜 하트가 깎였는지 알 수 없게 되므로, 예전 조립을 지우지 않고 남겨 뒀다.

어떤 얼굴이 나올지는 `HumanState.look` 이라는 **정수 하나**로 정해진다. 뽑기는
시스템이 시드 난수로 하고(§0-5 — 렌더에서 뽑으면 같은 시드가 같은 판을 만들지
않는다), 그 수를 얼굴로 바꾸는 일만 렌더 계층이 한다. 순수 로직은 얼굴이 몇
종류인지 알 필요가 없다 (§0-4).

---

## 알려진 문제

- **인간 적(§24)의 밸런스가 미해결이다.** 봇 측정에서 인간을 넣으면 신중한 플레이가
  5판 전부 아사한다 (인간 없이는 4/5 클리어). 사냥/휴식 주기(8초/25초), 추격 시간
  상한(5초), 시야 축소(6.0→3.5), 배고픔 회복 상향(10→13)을 모두 적용했으나 봇
  기준으로는 여전하다. 사인은 추격 자체가 아니라 **섭취 공백** 이다 — 배고픔 상한이
  100 이라 몰아 먹은 잉여가 버려지고, 피하는 동안의 긴 공백에서 0 이 된다.
  다만 이 봇은 쫓길 때마다 피난처로 가서 기다리는 과잉 대응을 한다. 인간이
  플레이어보다 느리므로(2.9 vs 3.2) 사람은 거리를 두며 계속 먹을 수 있어
  더 잘할 여지가 크다. **사람 플레이로 판단이 필요하다.**
  빼려면 `CONFIG.HUMAN_FROM_LEVEL` 을 99 로 두면 등장하지 않는다.
- 프로덕션 번들의 대부분이 Three.js 다 (604 kB / gzip 154 kB). 게임은 첫 화면부터
  Three.js 가 필요해 지연 로드할 여지가 없으므로 **전체 바이트는 줄지 않는다.**
  별도 청크로 분리해 둔 것은 캐시 분리 목적이다 — 게임 코드만 고치면 134 kB 만
  다시 받는다. `three/examples/jsm` 의 GLTFLoader·SkeletonUtils 도 같은 청크에
  넣는다 (48 kB) — 빼 두면 게임 코드 청크에 붙어서 캐시 분리가 무의미해진다.
- 헤드리스 브라우저(E2E)에서는 렌더가 느려 **시뮬레이션이 실시간보다 느리게 돈다.**
  게임 자체의 문제는 아니지만, E2E 에서 무언가를 기다릴 때 벽시계를 기준으로
  삼으면 안 된다. `e2e/helpers.ts` 의 `advanceGameTime` / `expectWithinGameTime` 을
  쓰면 예산을 게임 시간으로 줄 수 있다. (아래 "S9 에서 고친 것" 참조)

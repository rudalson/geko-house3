# AGENTS.md

이 저장소에서 작업하는 AI 어시스턴트 및 에이전트를 위한 지침서입니다.

---

## 1. Git 커밋 메시지 규칙

- **타이틀(첫 줄)**: 반드시 **영어**로 작성합니다.
  - Conventional Commits 형식을 따릅니다 (`fix: ...`, `feat: ...`, `refactor: ...`, `test: ...` 등).
  - 간결하고 명확하게 변경의 핵심을 설명합니다.
- **본문**: 반드시 **한국어로 자세히** 작성합니다.
  - 문제 상황 및 배경 설명
  - 발생 원인 분석
  - 해결 방식 및 구체적인 코드 변경 사항
  - 테스트 및 검증 결과 (통과한 테스트 개수, 빌드 검증 등)

---

## 2. 개발 및 검증 워크플로

1. **테스트 검증**:
   - 코드 수정 후 반드시 `npm test`를 실행하여 모든 단위 테스트가 통과하는지 확인합니다.
   - `npm run balance`를 실행하여 밸런스 검증이 합격하는지 확인합니다.
   - `npm run build`를 실행하여 TypeScript 컴파일 에러 및 번들링 문제가 없는지 확인합니다.
2. **설계 원칙 준수**:
   - `src/core/`와 `src/systems/`는 게임의 순수 로직 계층이며, Three.js나 DOM에 직접 의존하지 않습니다.
   - 렌더링 계층(`src/entities/`, `src/scenes/`)은 상태를 읽어 화면에 표현하는 단방향 의존성을 유지합니다.
